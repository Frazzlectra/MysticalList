<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_shoppinglist">

    <fragment
        android:id="@+id/nav_shoppinglist"
        android:name="mythicallist.main.ui.home.HomeFragment"
        android:label="@string/menu_shoppinglist"
        tools:layout="@layout/fragment_shoppinglist" />

    <fragment
        android:id="@+id/nav_myrecipes"
        android:name="mythicallist.main.ui.gallery.GalleryFragment"
        android:label="@string/menu_myrecipes"
        tools:layout="@layout/fragment_myrecipes" />

    <fragment
        android:id="@+id/nav_ingredients"
        android:name="mythicallist.main.ui.slideshow.SlideshowFragment"
        android:label="@string/menu_ingredients"
        tools:layout="@layout/fragment_ingredients" />
</navigation>